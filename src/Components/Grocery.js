const Geocery = () => {
    return(
        <div>
            <h1>Geolocation API</h1>
            <p>This app uses the browser's geolocation API to get the user's current location.</p>
        </div>
    )
};
export default Geocery;